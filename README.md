# 🧮 Calculadora de Áreas em Java

Este projeto implementa uma calculadora de áreas para diferentes figuras geométricas utilizando **polimorfismo**, **interface** e **records** em Java.  
O usuário pode escolher entre **Quadrado**, **Retângulo** e **Círculo**, informar os valores necessários e obter o cálculo da área diretamente no terminal.

---

## ✨ Funcionalidades
- Menu interativo via `Scanner` para escolher a figura geométrica.
- Implementação de interface `GeometricForm` para garantir polimorfismo.
- Classes representadas como `records` (`Square`, `Rectangle`, `Circle`).
- Cálculo automático da área de cada figura.
- Estrutura simples e extensível para adicionar novas formas geométricas.

---

## 📚 Conceitos aplicados
- Programação Orientada a Objetos (OOP)
- Polimorfismo
- Interfaces
- Records (Java 16+)
- Entrada de dados com `Scanner`

---

## 🚀 Como executar

### 🔹 Via Terminal
1. Certifique-se de ter o **Java JDK 17+** instalado e configurado no PATH.
2. Clone o repositório:
   ```bash
   git clone https://github.com/rafaelprisco23/CalculoDaArea.git
